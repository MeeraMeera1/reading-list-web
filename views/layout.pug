//- provides the overall HTML for each page 

doctype html
html(lang="en")
    head
        title Reading List - #{title}
    body
        h1 Reading List 
        div 
            h2 #{title}
            block content